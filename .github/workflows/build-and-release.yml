name: Build and Release

on:
  push:
    branches: ['*']
    tags: ['v*']
  pull_request:
    branches: ['*']

jobs:
  # Build and test job for all pushes/PRs
  build:
    name: Build and Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # Ubuntu 22.04 removed due to C++20 compatibility issues with GCC 11.4.0
        # Ubuntu 24.04 with GCC 13.3.0 provides full C++20 support required by the project
        os: [ubuntu-24.04]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Install build dependencies
      run: |
        sudo apt-get update
        # NB: Ubuntu 24.04 ships ImageMagick 6 as libmagick++-dev, whose Magick++ lacks
        # alpha()/evaluate()/2-arg addNoise() that the Imagick module needs. We build
        # ImageMagick 7 from source below instead, so do not install the distro package.
        sudo apt-get install -y build-essential cmake git pkg-config curl \
            libcurl4-openssl-dev libfmt-dev libmariadb-dev libfcgi-dev \
            libxml2-dev libmemcached-dev libarchive-dev libssl-dev \
            libmongoc-dev libbson-dev libsasl2-dev \
            libpng-dev libjpeg-dev libtiff-dev libwebp-dev libltdl-dev

    # MongoDB does not ship apt packages for the modern mongocxx (3.x/4.x) C++
    # driver, and Ubuntu 24.04 dropped `libmongocxx-dev` from its universe repo.
    # We build mongocxx from the official source release and cache the install
    # tree keyed on the version so we only pay the ~4-minute build on version
    # bumps.
    - name: Cache mongocxx install
      id: cache-mongocxx
      uses: actions/cache@v4
      with:
        path: /opt/mongocxx
        key: mongocxx-r4.3.0-${{ matrix.os }}

    - name: Build mongocxx from source
      if: steps.cache-mongocxx.outputs.cache-hit != 'true'
      run: |
        curl -fsSL -o /tmp/mongocxx.tar.gz \
          https://github.com/mongodb/mongo-cxx-driver/releases/download/r4.3.0/mongo-cxx-driver-r4.3.0.tar.gz
        tar -xzf /tmp/mongocxx.tar.gz -C /tmp
        cmake -S /tmp/mongo-cxx-driver-r4.3.0 -B /tmp/mongocxx-build \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_CXX_STANDARD=20 \
          -DCMAKE_INSTALL_PREFIX=/opt/mongocxx \
          -DBUILD_SHARED_LIBS=ON \
          -DENABLE_TESTS=OFF \
          -DBUILD_VERSION=4.3.0
        cmake --build /tmp/mongocxx-build --parallel
        cmake --install /tmp/mongocxx-build

    - name: Register mongocxx with pkg-config/ldconfig
      run: |
        echo "PKG_CONFIG_PATH=/opt/mongocxx/lib/pkgconfig" >> $GITHUB_ENV
        echo "/opt/mongocxx/lib" | sudo tee /etc/ld.so.conf.d/mongocxx.conf
        sudo ldconfig

    # Ubuntu 24.04 only packages ImageMagick 6, but the Imagick module uses Magick++
    # APIs that exist only in ImageMagick 7 (alpha(), evaluate(), 2-arg addNoise()).
    # Build IM7 from source, Q16 non-HDRI to match the module's compile definitions
    # (-DMAGICKCORE_QUANTUM_DEPTH=16 -DMAGICKCORE_HDRI_ENABLE=0), and cache the install.
    - name: Cache ImageMagick 7 install
      id: cache-imagemagick
      uses: actions/cache@v4
      with:
        path: /opt/imagemagick
        key: imagemagick-7.1.2-29-q16-nohdri-${{ matrix.os }}

    - name: Build ImageMagick 7 from source
      if: steps.cache-imagemagick.outputs.cache-hit != 'true'
      run: |
        curl -fsSL -o /tmp/imagemagick.tar.gz \
          https://github.com/ImageMagick/ImageMagick/archive/refs/tags/7.1.2-29.tar.gz
        tar -xzf /tmp/imagemagick.tar.gz -C /tmp
        cd /tmp/ImageMagick-7.1.2-29
        ./configure --prefix=/opt/imagemagick \
          --with-quantum-depth=16 --disable-hdri \
          --with-magick-plus-plus=yes \
          --without-x --disable-openmp
        make -j"$(nproc)"
        make install

    - name: Register ImageMagick 7 with the toolchain
      run: |
        echo "/opt/imagemagick/bin" >> $GITHUB_PATH
        echo "PKG_CONFIG_PATH=/opt/imagemagick/lib/pkgconfig:${PKG_CONFIG_PATH}" >> $GITHUB_ENV
        echo "CMAKE_PREFIX_PATH=/opt/imagemagick:${CMAKE_PREFIX_PATH}" >> $GITHUB_ENV
        echo "/opt/imagemagick/lib" | sudo tee /etc/ld.so.conf.d/imagemagick.conf
        sudo ldconfig

    - name: Configure CMake
      run: |
        cmake -B build \
          -DCMAKE_BUILD_TYPE=Release \
          -DBUILD_CLI=ON \
          -DBUILD_COMPILER=ON \
          -DBUILD_FASTCGI=ON \
          -DBUILD_TESTS=ON
    
    - name: Build project
      run: cmake --build build --parallel
    
    - name: Run tests
      run: |
        cd build
        ctest --output-on-failure --parallel
    
    - name: Create DEB packages (tags only)
      if: startsWith(github.ref, 'refs/tags/')
      run: |
        cd build
        cpack -G DEB || { echo "DEB creation failed"; exit 1; }
    
    - name: Upload DEB artifacts (tags only)
      if: startsWith(github.ref, 'refs/tags/') && hashFiles('build/*.deb') != ''
      uses: actions/upload-artifact@v4
      with:
        name: voidscript-deb-${{ matrix.os }}-${{ github.sha }}
        path: build/*.deb
        retention-days: 7
        if-no-files-found: error

  # Release job for tagged versions only
  release:
    name: Create GitHub Release
    runs-on: ubuntu-24.04
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    
    steps:
    - name: Download DEB artifacts
      uses: actions/download-artifact@v4
      with:
        name: voidscript-deb-ubuntu-24.04-${{ github.sha }}
        path: artifacts/
    
    - name: Get tag name
      id: get_tag
      run: echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
    
    - name: List available packages
      run: |
        echo "Available DEB packages for release:"
        ls -la artifacts/*.deb || echo "No .deb files found in artifacts"
    
    - name: Create GitHub Release
      if: hashFiles('artifacts/*.deb') != ''
      uses: softprops/action-gh-release@v2
      with:
        name: ${{ steps.get_tag.outputs.tag }}
        tag_name: ${{ steps.get_tag.outputs.tag }}
        draft: false
        prerelease: false
        generate_release_notes: true
        files: artifacts/*.deb
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
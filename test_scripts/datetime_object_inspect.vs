print("=== DateTime Object Inspection ===");

DateTime $dt = new DateTime();
print("DateTime object created");

// Check the type
print("Object type: " + typeof($dt));

// Try to access the object as a map to see what's in it
// This is a debugging approach - not typical usage
print("Debug: Object contents inspection complete");